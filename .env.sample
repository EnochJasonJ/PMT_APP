# App configuration
DEBUG=True
APP_ENV=development

# Database configuration
DB_NAME=scheduler
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
DB_HOST=localhost

# REDIS
REDIS_PASSWORD=hOW32#$@41u31Aq4
REDIS_PORT=6379
# Encryption (secrets at rest). Generate your own; do NOT reuse.
ENCRYPTION_KEY=replace-with-fernet-key

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=invites@appxcess.com
APP_BASE_URL=https://your-app-url
INVITE_EXPIRE_HOURS=72
